/target

# Synthesis output; the CLI writes out.wav by default.
*.wav

# Editor and IDE state.
/.idea

# Python bytecode from tools/
__pycache__/
*.pyc

# The upstream Flite reference build, which is produced locally from a source
# tree this repository does not carry.
/tools/reference/build/